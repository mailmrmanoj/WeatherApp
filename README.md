# WeatherApp

Steps to run :

1. Download project
2. Navigate to root directory(WeatherApp)
3. In the command prompt 
                       > `npm install`
                       > `bower install`
                       > `grunt start`
